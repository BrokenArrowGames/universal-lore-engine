{
  "name": "universal-lore-engine",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.mjs",
    "start:dev": "npm run -s gulp -- watch & nodemon -e mjs --watch src src/index.mjs",
    "compile": "npm run -s gulp -- clean build",
    "gulp": "gulp --gulpfile config/gulpfile.mjs --cwd $INIT_CWD"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^5.0.0",
    "gulp-cli": "^3.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-front-matter": "^1.3.0",
    "gulp-markdown": "^8.0.0",
    "gulp-rename": "^2.0.0",
    "nodemon": "^3.1.2"
  },
  "dependencies": {
    "express": "^4.19.2",
    "liquidjs": "^10.13.1"
  }
}
